{
    "title" : "BuyerNotes" , 
    "description" : "Buyer Notes",
    "type" : "object" ,
    "properties" : {
        "buyer_id" : {
            "type" : "number"
        },
        "note_date" : {
            "type" : "string",
            "format" : "date-time"
        },
        "note_by" : {
            "type" : "string",
            "maxLength" : 25
        },
        "note_text" : {
            "type " : "string",
            "maxLength" : 255
        },
        "topic" : {
            "type" : "string",
            "maxLength" : 150
        }
    },
    "required": ["buyer_id", "note_date"  ]
}